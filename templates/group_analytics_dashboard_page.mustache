{{!
    This file is part of Moodle - http://moodle.org/

    Moodle is free software: you can redistribute it and/or modify
    it under the terms of the GNU General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    Moodle is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU General Public License for more details.

    @template local_comp_report_ext/group_analytics_dashboard_page
}}
<div class="local_comp_report_ext_analytics_dashboard">
    <!-- Navigation Tabs -->
    <ul class="nav nav-tabs mb-4">
        <li class="nav-item">
            <a class="nav-link font-weight-bold text-muted" href="group_competency.php?courseid={{courseid}}">
                <i class="fa fa-list-alt mr-1"></i> {{#str}}tab_by_competency, local_comp_report_ext{{/str}}
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link font-weight-bold text-muted" href="group_quiz_competency.php?courseid={{courseid}}">
                <i class="fa fa-pencil-square-o mr-1"></i> {{#str}}tab_by_quiz, local_comp_report_ext{{/str}}
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link font-weight-bold text-muted" href="group_assessment_distribution.php?courseid={{courseid}}">
                <i class="fa fa-balance-scale mr-1"></i> {{#str}}tab_assessment_distribution, local_comp_report_ext{{/str}}
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link active font-weight-bold" href="#">
                <i class="fa fa-bar-chart mr-1"></i> {{#str}}tab_analytics_competency, local_comp_report_ext{{/str}}
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link font-weight-bold text-muted" href="group_exam_analytics.php?courseid={{courseid}}">
                <i class="fa fa-line-chart mr-1"></i> {{#str}}tab_analytics_grades, local_comp_report_ext{{/str}}
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link font-weight-bold text-muted" href="term_comprehensive_report.php?courseid={{courseid}}">
                <i class="fa fa-graduation-cap mr-1"></i> {{#str}}tab_term_comprehensive, local_comp_report_ext{{/str}}
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link font-weight-bold text-muted" href="trainer_analytics.php?courseid={{courseid}}">
                <i class="fa fa-user-circle-o mr-1"></i> {{#str}}tab_trainer_analytics, local_comp_report_ext{{/str}}
            </a>
        </li>
    </ul>

    <!-- Filter Form -->
    <form method="get" class="form-inline mb-4 bg-light p-3 rounded shadow-sm">
        <input type="hidden" name="courseid" value="{{courseid}}">
        <div class="form-group mr-3">
            <label class="font-weight-bold mr-2 mb-0"><i class="fa fa-users mr-1"></i> {{#str}}group, local_comp_report_ext{{/str}}</label>
            <select name="groupid" class="form-control" onchange="this.form.submit()">
                {{#groups}}
                    <option value="{{id}}" {{#selected}}selected{{/selected}}>{{name}}</option>
                {{/groups}}
            </select>
        </div>

        <button type="submit" class="btn btn-primary"><i class="fa fa-eye mr-1"></i> {{strshowstudents}}</button>
        {{#has_data}}
        <button id="btn-print-analytics" type="button" class="btn btn-outline-primary font-weight-bold shadow-sm ml-2">
            <i class="fa fa-print mr-1"></i> {{strprintreport}}
        </button>
        {{/has_data}}
    </form>

    {{^has_data}}
        <div class="alert alert-info py-5 text-center shadow-sm">
            <i class="fa fa-bar-chart fa-3x mb-3 opacity-50 d-block"></i>
            <p class="mb-0">{{strnodata}}</p>
        </div>
    {{/has_data}}

    {{#has_data}}
    <div id="analytics-dashboard-content">
    <!-- KPI Overview Cards (Clickable for Drill-down) -->
    <div class="row mb-4">
        <!-- Average Mastery Card -->
        <div class="col-md-3 mb-3">
            <div class="card h-100 border-0 shadow-sm kpi-card bg-mastery text-white" id="btn-kpi-all-students" style="cursor: pointer;" title="Click to view all student scores">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <span class="text-uppercase small font-weight-bold opacity-75">{{#str}}average_mastery_rate, local_comp_report_ext{{/str}}</span>
                        <i class="fa fa-graduation-cap fa-lg opacity-75"></i>
                    </div>
                    <h2 class="mb-0 font-weight-bold">{{avg_mastery}}%</h2>
                    <small class="opacity-75 d-block mt-2"><i class="fa fa-search-plus mr-1"></i> Click for student roster</small>
                </div>
            </div>
        </div>
        <!-- Remediation Card -->
        <div class="col-md-3 mb-3">
            <div class="card h-100 border-0 shadow-sm kpi-card bg-remediation text-white" id="btn-kpi-remediation" style="cursor: pointer;" title="Click to view students needing remediation">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <span class="text-uppercase small font-weight-bold opacity-75">{{#str}}remediation_rate, local_comp_report_ext{{/str}}</span>
                        <i class="fa fa-exclamation-triangle fa-lg opacity-75"></i>
                    </div>
                    <h2 class="mb-0 font-weight-bold">{{remediation_rate}}%</h2>
                    <small class="opacity-75 d-block mt-2"><i class="fa fa-user-times mr-1"></i> Click for remediation list</small>
                </div>
            </div>
        </div>
        <!-- Top Strength Card -->
        <div class="col-md-3 mb-3">
            <div class="card h-100 border-0 shadow-sm kpi-card bg-strength text-white">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <span class="text-uppercase small font-weight-bold opacity-75">{{#str}}top_strength, local_comp_report_ext{{/str}}</span>
                        <i class="fa fa-thumbs-o-up fa-lg opacity-75"></i>
                    </div>
                    <h5 class="mb-0 font-weight-bold text-truncate" title="{{top_strength}}">{{top_strength}}</h5>
                </div>
            </div>
        </div>
        <!-- Critical Gap Card -->
        <div class="col-md-3 mb-3">
            <div class="card h-100 border-0 shadow-sm kpi-card bg-gap text-white">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <span class="text-uppercase small font-weight-bold opacity-75">{{#str}}critical_gap, local_comp_report_ext{{/str}}</span>
                        <i class="fa fa-bolt fa-lg opacity-75"></i>
                    </div>
                    <h5 class="mb-0 font-weight-bold text-truncate" title="{{critical_gap}}">{{critical_gap}}</h5>
                </div>
            </div>
        </div>
    </div>

    <!-- Charts Row 1: Competency Mastery Radar & Mastery Distribution -->
    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-white font-weight-bold border-bottom-0 pb-0 pt-3">
                    <i class="fa fa-star text-warning mr-1"></i> {{#str}}competency_mastery_radar, local_comp_report_ext{{/str}}
                </div>
                <div class="card-body p-3" style="position: relative; height: 360px; width: 100%;">
                    <canvas id="chart-competency-radar"></canvas>
                </div>
            </div>
        </div>
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-white font-weight-bold border-bottom-0 pb-0 pt-3 d-flex justify-content-between align-items-center">
                    <span><i class="fa fa-pie-chart text-info mr-1"></i> {{#str}}mastery_distribution, local_comp_report_ext{{/str}}</span>
                    <small class="text-muted"><i class="fa fa-hand-pointer-o mr-1"></i> Click any bar to drill down</small>
                </div>
                <div class="card-body p-3" style="position: relative; height: 360px; width: 100%;">
                    <canvas id="chart-mastery-distribution" style="cursor:pointer;"></canvas>
                </div>
            </div>
        </div>
    </div>

    <!-- Charts Row 2: Score Distribution Histogram & Theory vs Practice -->
    <div class="row">
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-white font-weight-bold border-bottom-0 pb-0 pt-3 d-flex justify-content-between align-items-center">
                    <span><i class="fa fa-bar-chart text-success mr-1"></i> {{#str}}score_distribution_histogram, local_comp_report_ext{{/str}}</span>
                    <small class="text-muted"><i class="fa fa-hand-pointer-o mr-1"></i> Click any bar to drill down</small>
                </div>
                <div class="card-body p-3" style="position: relative; height: 360px; width: 100%;">
                    <canvas id="chart-learning-progress" style="cursor:pointer;"></canvas>
                </div>
            </div>
        </div>
        <div class="col-md-6 mb-4">
            <div class="card shadow-sm h-100">
                <div class="card-header bg-white font-weight-bold border-bottom-0 pb-0 pt-3">
                    <i class="fa fa-columns text-primary mr-1"></i> {{#str}}theory_vs_practice, local_comp_report_ext{{/str}}
                </div>
                <div class="card-body p-3" style="position: relative; height: 360px; width: 100%;">
                    <canvas id="chart-theory-practice"></canvas>
                </div>
            </div>
        </div>
    </div>

    <!-- Student Cohort Roster & Interactive Breakdown Table -->
    <div class="card shadow-sm mt-4 border-0 mb-4" id="section-student-roster">
        <div class="card-header bg-white border-bottom-0 pt-4 pb-2 d-flex justify-content-between align-items-center flex-wrap">
            <h5 class="font-weight-bold text-dark mb-2">
                <i class="fa fa-users text-primary mr-2"></i> Student Roster & Performance Breakdown
            </h5>
            <!-- Filter Buttons -->
            <div class="btn-group btn-group-sm mb-2" id="roster-filter-group" role="group">
                <button type="button" class="btn btn-outline-secondary active" data-filter="all">All</button>
                <button type="button" class="btn btn-outline-danger" data-filter="remediation"><i class="fa fa-exclamation-triangle mr-1"></i> Remediation Required</button>
                <button type="button" class="btn btn-outline-danger" data-filter="critical">Critical (&lt;40%)</button>
                <button type="button" class="btn btn-outline-warning" data-filter="developing">Developing (40–59%)</button>
                <button type="button" class="btn btn-outline-primary" data-filter="proficient">Proficient (60–79%)</button>
                <button type="button" class="btn btn-outline-success" data-filter="exemplary">Exemplary (80–100%)</button>
            </div>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover table-striped mb-0" id="table-student-roster">
                    <thead class="thead-light">
                        <tr>
                            <th class="pl-4">#</th>
                            <th>Student Name</th>
                            <th class="text-center">Average Mastery (%)</th>
                            <th class="text-center">Performance Status</th>
                            <th class="text-center">Remediation Status</th>
                            <th class="text-right pr-4">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#student_list}}
                        <tr class="roster-row" data-tier="{{tier}}" data-remediation="{{#needs_remediation}}1{{/needs_remediation}}{{^needs_remediation}}0{{/needs_remediation}}" data-decile="{{decile_bin}}">
                            <td class="pl-4 align-middle text-muted small">{{index}}</td>
                            <td class="font-weight-bold align-middle">{{fullname}}</td>
                            <td class="text-center align-middle font-weight-bold" style="font-size: 1.05rem;">{{average}}%</td>
                            <td class="text-center align-middle">
                                <span class="badge {{badge_class}} px-2 py-1" style="font-size: 0.85rem;">{{tier_name}}</span>
                            </td>
                            <td class="text-center align-middle">
                                {{#needs_remediation}}
                                    <span class="badge badge-danger px-2 py-1" style="font-size: 0.85rem;"><i class="fa fa-exclamation-triangle mr-1"></i> Remediation Required</span>
                                {{/needs_remediation}}
                                {{^needs_remediation}}
                                    <span class="badge badge-success px-2 py-1" style="font-size: 0.85rem;"><i class="fa fa-check mr-1"></i> On Track</span>
                                {{/needs_remediation}}
                            </td>
                            <td class="text-right pr-4 align-middle">
                                <a href="{{detail_url}}" class="btn btn-sm btn-outline-primary font-weight-bold">
                                    <i class="fa fa-file-text-o mr-1"></i> View Report
                                </a>
                            </td>
                        </tr>
                        {{/student_list}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- AI Commentary Widget -->
    {{#has_data}}
        {{> local_comp_report_ext/ai_commentary_widget }}
    {{/has_data}}
    </div><!-- /#analytics-dashboard-content -->
    {{/has_data}}
</div>

<!-- Student Drill-down Modal -->
<div class="modal fade" id="modal-student-drilldown" tabindex="-1" role="dialog" aria-labelledby="modalDrilldownLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered" role="document">
        <div class="modal-content shadow-lg border-0" style="border-radius: 12px; overflow: hidden;">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title font-weight-bold" id="modalDrilldownLabel">
                    <i class="fa fa-users mr-2"></i> <span id="modal-drilldown-title">Student Drill-down</span>
                </h5>
                <button type="button" class="close text-white opacity-100" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body p-0">
                <div class="table-responsive">
                    <table class="table table-hover table-striped mb-0">
                        <thead class="thead-light">
                            <tr>
                                <th class="pl-4">Student Name</th>
                                <th class="text-center">Average Score</th>
                                <th class="text-center">Performance Status</th>
                                <th class="text-right pr-4">Action</th>
                            </tr>
                        </thead>
                        <tbody id="modal-drilldown-tbody">
                            <!-- Populated dynamically via JS -->
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="modal-footer bg-light">
                <button type="button" class="btn btn-secondary font-weight-bold" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

{{#has_data}}
{{#js}}
require(['core/chartjs', 'jquery'], function(Chart, $) {

    var studentsData = {{{student_list_json}}};

    // Modal Helper Function
    function openDrilldownModal(title, filteredStudents) {
        $('#modal-drilldown-title').text(title);
        var tbody = $('#modal-drilldown-tbody');
        tbody.empty();

        if (!filteredStudents || filteredStudents.length === 0) {
            tbody.append('<tr><td colspan="4" class="text-center text-muted py-4"><i class="fa fa-info-circle mr-1"></i> No students found in this category.</td></tr>');
        } else {
            $.each(filteredStudents, function(i, s) {
                var row = $('<tr>');
                row.append('<td class="pl-4 font-weight-bold align-middle">' + s.fullname + '</td>');
                row.append('<td class="text-center align-middle font-weight-bold">' + s.average + '%</td>');
                row.append('<td class="text-center align-middle"><span class="badge ' + s.badge_class + ' px-2 py-1">' + s.tier_name + '</span></td>');
                row.append('<td class="text-right pr-4 align-middle"><a href="' + s.detail_url + '" class="btn btn-sm btn-outline-primary font-weight-bold"><i class="fa fa-file-text-o mr-1"></i> View Report</a></td>');
                tbody.append(row);
            });
        }

        $('#modal-student-drilldown').modal('show');
    }

    // Roster Filter Helper
    function filterRosterTable(filterType) {
        $('#roster-filter-group button').removeClass('active');
        $('#roster-filter-group button[data-filter="' + filterType + '"]').addClass('active');

        $('.roster-row').each(function() {
            var $row = $(this);
            if (filterType === 'all') {
                $row.show();
            } else if (filterType === 'remediation') {
                if ($row.data('remediation') == 1) { $row.show(); } else { $row.hide(); }
            } else if (filterType.indexOf('decile_') === 0) {
                var decile = filterType.replace('decile_', '');
                if ($row.data('decile') == decile) { $row.show(); } else { $row.hide(); }
            } else {
                if ($row.data('tier') === filterType) { $row.show(); } else { $row.hide(); }
            }
        });
    }

    // Filter Button Click Handlers
    $('#roster-filter-group button').on('click', function() {
        var filter = $(this).data('filter');
        filterRosterTable(filter);
    });

    // KPI Cards Click Handlers
    $('#btn-kpi-remediation').on('click', function() {
        var list = studentsData.filter(function(s) { return s.needs_remediation; });
        openDrilldownModal('Students Requiring Remediation (' + list.length + ')', list);
        filterRosterTable('remediation');
    });

    $('#btn-kpi-all-students').on('click', function() {
        openDrilldownModal('All Students (' + studentsData.length + ')', studentsData);
        filterRosterTable('all');
    });

    // Print Report
    $('#btn-print-analytics').on('click', function(e) {
        e.preventDefault();
        window.print();
    });

    // 1. Radar Chart Setup
    var radarCtx = document.getElementById('chart-competency-radar').getContext('2d');
    new Chart(radarCtx, {
        type: 'radar',
        data: {
            labels: {{{radar_labels_json}}},
            datasets: [{
                label: '{{#str}}average_mastery_rate, local_comp_report_ext{{/str}} (%)',
                data: {{{radar_data_json}}},
                backgroundColor: 'rgba(59, 130, 246, 0.2)',
                borderColor: '#3B82F6',
                borderWidth: 2,
                pointBackgroundColor: '#2563EB',
                pointBorderColor: '#fff',
                pointHoverBackgroundColor: '#fff',
                pointHoverBorderColor: '#2563EB'
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            scales: {
                r: {
                    min: 0,
                    max: 100,
                    ticks: {
                        beginAtZero: true,
                        stepSize: 20
                    }
                }
            }
        }
    });

    // 2. Mastery Distribution Bar Chart with Click Drill-down
    var distCtx = document.getElementById('chart-mastery-distribution').getContext('2d');
    new Chart(distCtx, {
        type: 'bar',
        data: {
            labels: [
                '{{#str}}critical_tier, local_comp_report_ext{{/str}}',
                '{{#str}}developing_tier, local_comp_report_ext{{/str}}',
                '{{#str}}proficient_tier, local_comp_report_ext{{/str}}',
                '{{#str}}exemplary_tier, local_comp_report_ext{{/str}}'
            ],
            datasets: [{
                data: {{{dist_data_json}}},
                backgroundColor: [
                    'rgba(239, 68, 68, 0.75)',
                    'rgba(245, 158, 11, 0.75)',
                    'rgba(59, 130, 246, 0.75)',
                    'rgba(16, 185, 129, 0.75)'
                ],
                borderColor: [
                    '#EF4444',
                    '#F59E0B',
                    '#3B82F6',
                    '#10B981'
                ],
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: { display: false }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    ticks: {
                        stepSize: 1
                    }
                }
            },
            onClick: function(evt, activeElements) {
                if (activeElements && activeElements.length > 0) {
                    var index = activeElements[0].index;
                    var tiers = ['critical', 'developing', 'proficient', 'exemplary'];
                    var tierTitles = ['Critical Students (< 40%)', 'Developing Students (40–59%)', 'Proficient Students (60–79%)', 'Exemplary Students (80–100%)'];
                    var targetTier = tiers[index];
                    var title = tierTitles[index];

                    var list = studentsData.filter(function(s) { return s.tier === targetTier; });
                    openDrilldownModal(title + ' (' + list.length + ')', list);
                    filterRosterTable(targetTier);
                }
            }
        }
    });

    // 3. Score Distribution Histogram Bar Chart with Click Drill-down
    var progressCtx = document.getElementById('chart-learning-progress').getContext('2d');
    new Chart(progressCtx, {
        type: 'bar',
        data: {
            labels: {{{histogram_labels_json}}},
            datasets: [{
                label: '{{#str}}user, local_comp_report_ext{{/str}}',
                data: {{{histogram_data_json}}},
                backgroundColor: [
                    'rgba(220, 38, 38, 0.75)',
                    'rgba(239, 68, 68, 0.75)',
                    'rgba(249, 115, 22, 0.75)',
                    'rgba(245, 158, 11, 0.75)',
                    'rgba(234, 179, 8, 0.75)',
                    'rgba(202, 138, 4, 0.75)',
                    'rgba(59, 130, 246, 0.75)',
                    'rgba(37, 99, 235, 0.75)',
                    'rgba(16, 185, 129, 0.75)',
                    'rgba(5, 150, 105, 0.75)'
                ],
                borderColor: [
                    '#DC2626',
                    '#EF4444',
                    '#F97316',
                    '#F59E0B',
                    '#EAB308',
                    '#CA8A04',
                    '#3B82F6',
                    '#2563EB',
                    '#10B981',
                    '#059669'
                ],
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: { display: false }
            },
            scales: {
                y: {
                    beginAtZero: true,
                    ticks: {
                        stepSize: 1
                    }
                }
            },
            onClick: function(evt, activeElements) {
                if (activeElements && activeElements.length > 0) {
                    var index = activeElements[0].index;
                    var labels = ['0-10%', '11-20%', '21-30%', '31-40%', '41-50%', '51-60%', '61-70%', '71-80%', '81-90%', '91-100%'];
                    var binLabel = labels[index];

                    var list = studentsData.filter(function(s) { return s.decile_bin === index; });
                    openDrilldownModal('Students with Score ' + binLabel + ' (' + list.length + ')', list);
                    filterRosterTable('decile_' + index);
                }
            }
        }
    });

    // 4. Theory vs Practice Bar Chart
    var gapCtx = document.getElementById('chart-theory-practice').getContext('2d');
    new Chart(gapCtx, {
        type: 'bar',
        data: {
            labels: {{{gap_labels_json}}},
            datasets: [
                {
                    label: '{{#str}}tab_by_quiz, local_comp_report_ext{{/str}} (%)',
                    data: {{{gap_theory_json}}},
                    backgroundColor: 'rgba(59, 130, 246, 0.7)',
                    borderColor: '#3B82F6',
                    borderWidth: 1
                },
                {
                    label: '{{#str}}practicalentry, local_comp_report_ext{{/str}} (%)',
                    data: {{{gap_practice_json}}},
                    backgroundColor: 'rgba(16, 185, 129, 0.7)',
                    borderColor: '#10B981',
                    borderWidth: 1
                }
            ]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            scales: {
                y: {
                    min: 0,
                    max: 100,
                    ticks: {
                        stepSize: 20
                    }
                }
            }
        }
    });
});
{{/js}}
{{/has_data}}
